import styled from 'styled-components';

import { lighterColor } from '../../styles/tokens';

const Wrapper = styled.div`
    display: flex;
    align-items: center;
    padding: 1rem;
    background-color: ${lighterColor}
`;

export default Wrapper;
